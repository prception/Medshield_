# Working backups - never deploy these (Google would index them as duplicates)
*.bak
*bak
*.seobak
*.pre-*
*.csxsafe
# ...but the asset backups under assets/ are deliberately kept
!assets/**/*bak*

# Local debug / scratch
_dbg.js
_t.html
probe*.cjs
p2.cjs
p3.cjs

# Dependencies
node_modules/

# Client source material - kept locally, never committed or deployed
client contents/

# Reference site rips (other studios' HTML - do not publish)
obscura.html
wd.html
tmc.html

# Editor/OS cruft
Thumbs.db
_Thumbs.db
.DS_Store

# Earlier stills for the Why Us section, referenced by nothing. Kept locally.
assets/why-us/[0-9].png

# video.mp4 is the ENCODE SOURCE for the Why Us reel, not a deployed asset:
# scrub-1280.mp4 and scrub-720.mp4 are generated from it (see why-scrub.js
# for the ffmpeg commands). Kept locally so the reel can be re-encoded;
# never deployed.
assets/why-us/video.mp4
